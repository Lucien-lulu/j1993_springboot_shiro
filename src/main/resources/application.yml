
server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/xiaoshu?characterEncoding=utf8&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true          #控制台打印sql
  thymeleaf:                #建议开发关闭缓存
    cache: false
    mode: LEGACYHTML5         #去除thymeleaf的html严格校验
